function Header({
    title,
    subtitle,
    description
}) {
    return (
        <header className="header">

            <h1>
                Analytics Portal
            </h1>

            <div>
                Welcome, User
            </div>

        </header>
    );
}

export default Header;